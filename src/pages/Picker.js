import { Typography } from "@mui/material";
import React from "react";

const Picker = () => {
  return (
    <div>
      <Typography>Picker page!</Typography>
    </div>
  );
};

export default Picker;
