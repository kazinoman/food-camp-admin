import { Typography } from "@mui/material";
import React from "react";

const Camp = () => {
  return (
    <div>
      <Typography>Camp details page!</Typography>
    </div>
  );
};

export default Camp;
